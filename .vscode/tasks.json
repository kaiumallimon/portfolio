{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Install @types/three and build",
			"type": "shell",
			"command": "npm install --save-dev @types/three && npm run build",
			"group": "build"
		}
	]
}