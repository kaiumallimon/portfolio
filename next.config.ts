import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // avatars.githubusercontent.com is used for GitHub profile images
  images: {
    domains: ["avatars.githubusercontent.com"],
  },
};

export default nextConfig;
